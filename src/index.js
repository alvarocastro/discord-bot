import Bot from './bot';
import Command from './command';
import Memory from './memory';
import commands from './commands';

export default {
	Bot,
	Command,
	Memory,
	commands
};

export { Bot };
export { Command };
export { Memory };
export { commands };
